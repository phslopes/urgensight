# DVC, uv e Python 3.12 — Plano de Implementação

> **Para agentes executores:** SUB-SKILL OBRIGATÓRIA: use
> `superpowers:subagent-driven-development` (recomendado) ou
> `superpowers:executing-plans` para executar tarefa a tarefa. Os passos usam
> checkbox (`- [ ]`) para rastreamento.

**Goal:** Estabelecer uma única fonte de verdade para dependências, versão de
Python e parâmetros de treino do UrgenSight, eliminando a divergência de
`numpy` entre o ambiente de treino (Airflow) e o de inferência (API).

**Architecture:** Três commits sequenciais e independentemente revertíveis —
(1) piso de Python 3.12, (2) `uv` com grupos PEP 735 substituindo `pip`,
(3) DVC como pipeline reprodutível orquestrado pelo Airflow. O commit 3 tem
gate de aborto: se falhar, é revertido sem levar os anteriores junto.

**Tech Stack:** Python 3.12, uv 0.11+, DVC 3.67+, Apache Airflow 2.9.3,
scikit-learn 1.5.1, FastAPI, GitHub Actions, Docker.

**Spec:** `docs/ai/specs/2026-09-02-dvc-uv-python312-design.md`

## Global Constraints

Aplicam-se a **todas** as tarefas:

- **Sem atribuição de IA em commits.** Nenhum trailer `Co-Authored-By:`,
  `Claude-Session:` ou rodapé "🤖 Generated with Claude Code". Mensagens em
  **Conventional Commits PT-BR** (`feat(escopo):`, `fix(escopo):`,
  `docs(escopo):`, `chore(escopo):`, `refactor(escopo):`).
- **Piso de Python:** `>=3.12`. Alvo do ruff: `py312`.
- **Pins de compatibilidade de pickle** (ditados pelas constraints oficiais do
  Airflow 2.9.3): `numpy==1.26.4`, `pandas==2.1.4`, `scikit-learn==1.5.1`.
- **Imagem do Airflow:** `apache/airflow:2.9.3-python3.12`; constraints em
  `https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.12.txt`.
- **Remote do DVC:** `.dvcstore/`, dentro do repositório, gitignored.
- **`dvc` nunca entra na imagem da API** — pertence ao grupo `pipeline`.
- **Não reescrever** `docs/ai/plans/*` nem `docs/ai/specs/2026-08-*` e
  `2026-09-01-*`: são registro histórico.
- **Antes de cada commit:** `make lint` e `make test` verdes. Sem exceção.
- **Idioma:** todo código, comentário e documentação em PT-BR (sem acentos em
  código-fonte, seguindo o padrão existente em `src/`).

---

# COMMIT 1 — Piso de Python 3.12

---

### Task 1: Piso de Python 3.12 e teste de coerência de ambiente

Esta é a tarefa que cria a rede de proteção usada por todas as seguintes: um
teste que falha quando as versões declaradas em `pyproject.toml`,
`.python-version`, `Dockerfile`, `Dockerfile.airflow` e `ci.yml` divergem
entre si. A ausência dessa checagem é a causa-raiz descrita na §1.2 da spec.

**Files:**
- Create: `.python-version`
- Create: `tests/test_environment_consistency.py`
- Modify: `pyproject.toml:7` (`requires-python`), `pyproject.toml:28`
  (`[tool.ruff] target-version`), e adicionar `[tool.uv]`
- Modify: `uv.lock` (relock)

**Interfaces:**
- Consumes: nada (primeira tarefa).
- Produces: `tests/test_environment_consistency.py` com as funções auxiliares
  `read_pyproject() -> dict`, `read_text(relative_path: str) -> str` e a
  constante `PROJECT_ROOT: Path`. As tarefas 4 e 9 estendem este arquivo.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/test_environment_consistency.py`:

```python
"""Coerencia de versoes entre os ambientes do projeto.

O modelo e treinado no container do Airflow e servido pela API. Quando as
versoes de Python ou das bibliotecas que atravessam o pickle divergem entre
esses ambientes, a falha aparece so em runtime, ao desserializar
models/model.pkl. Estes testes transformam essa divergencia em erro de
suite.
"""

import re
import tomllib
from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parent.parent

PYTHON_MAJOR_MINOR = "3.12"


def read_pyproject() -> dict:
    return tomllib.loads((PROJECT_ROOT / "pyproject.toml").read_text("utf-8"))


def read_text(relative_path: str) -> str:
    return (PROJECT_ROOT / relative_path).read_text("utf-8")


class TestPythonFloor:
    def test_pyproject_requires_python_312(self):
        assert read_pyproject()["project"]["requires-python"] == ">=3.12"

    def test_ruff_targets_py312(self):
        assert read_pyproject()["tool"]["ruff"]["target-version"] == "py312"

    def test_python_version_file_matches_floor(self):
        assert read_text(".python-version").strip() == PYTHON_MAJOR_MINOR

    def test_api_dockerfile_uses_same_python(self):
        assert f"FROM python:{PYTHON_MAJOR_MINOR}-slim" in read_text("Dockerfile")

    def test_airflow_dockerfile_uses_same_python(self):
        content = read_text("Dockerfile.airflow")
        assert f"python{PYTHON_MAJOR_MINOR}" in content

    def test_airflow_constraints_match_airflow_python(self):
        """A URL de constraints precisa acompanhar a tag da imagem."""
        content = read_text("Dockerfile.airflow")
        image = re.search(r"FROM apache/airflow:([\d.]+)-python([\d.]+)", content)
        assert image is not None, "tag da imagem do Airflow nao reconhecida"
        airflow_version, python_version = image.group(1), image.group(2)
        expected = (
            f"constraints-{airflow_version}/constraints-{python_version}.txt"
        )
        assert expected in content

    def test_ci_uses_same_python(self):
        assert f'PYTHON_VERSION: "{PYTHON_MAJOR_MINOR}"' in read_text(
            ".github/workflows/ci.yml"
        )


class TestUvProjectLayout:
    def test_project_is_declared_virtual(self):
        """Sem [build-system], o uv precisa saber que nao ha pacote a instalar."""
        assert read_pyproject()["tool"]["uv"]["package"] is False
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v`
Expected: FAIL — `test_pyproject_requires_python_312` (hoje é `>=3.10`),
`test_ruff_targets_py312` (hoje `py310`), `test_python_version_file_matches_floor`
(arquivo inexistente), `test_airflow_dockerfile_uses_same_python` (hoje 3.11) e
`test_project_is_declared_virtual` (sem `[tool.uv]`).

- [ ] **Step 3: Criar `.python-version`**

```
3.12
```

- [ ] **Step 4: Ajustar `pyproject.toml`**

Trocar `requires-python = ">=3.10"` por:

```toml
requires-python = ">=3.12"
```

Trocar `target-version = "py310"` por:

```toml
target-version = "py312"
```

Adicionar ao final do arquivo:

```toml
[tool.uv]
# Projeto sem [build-system]: nao ha pacote a construir/instalar, apenas
# um ambiente a sincronizar. uv.lock ja registra source = { virtual = "." };
# esta chave torna isso explicito e estavel entre versoes do uv.
package = false
```

- [ ] **Step 5: Atualizar `Dockerfile.airflow`**

Trocar a linha 4 e a URL de constraints da linha 11:

```dockerfile
FROM apache/airflow:2.9.3-python3.12
```

```dockerfile
RUN pip install --no-cache-dir -r /requirements.txt \
    --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.12.txt"
```

(A troca de `pip` por `uv` acontece na Task 5; aqui muda só o Python.)

- [ ] **Step 6: Relock e sincronizar**

```bash
uv lock && uv sync
```

- [ ] **Step 7: Rodar os testes e verificar que passam**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v`
Expected: PASS (todos)

- [ ] **Step 8: Rodar a suíte completa e o lint**

Run: `make test && make lint`
Expected: PASS. `make lint` pode acusar novos avisos do ruff por causa do
`target-version = "py312"` (regras `UP` passam a sugerir sintaxe mais nova).
Se acusar, rodar `make format` e revisar o diff antes de seguir.

- [ ] **Step 9: Commit**

```bash
git add .python-version pyproject.toml uv.lock Dockerfile.airflow \
        tests/test_environment_consistency.py
git commit -m "chore(python): elevar piso do projeto para Python 3.12

Alinha requires-python, target do ruff, .python-version e a imagem do
Airflow em 3.12, e adiciona teste de coerencia que falha quando esses
pontos divergem entre si."
```

---

### Task 2: Portão de validação do Airflow em 3.12

Esta tarefa **não altera arquivos** — é o portão exigido pelo Q7 da spec. Ela
decide se o commit 1 permanece como está ou se o Airflow volta para 3.11.

**Files:**
- Modify (somente se o portão falhar): `Dockerfile.airflow`

**Interfaces:**
- Consumes: `Dockerfile.airflow` na versão 3.12 (Task 1).
- Produces: decisão registrada; nenhum símbolo novo.

- [ ] **Step 1: Buildar a imagem do Airflow**

Run: `docker build -f Dockerfile.airflow -t urgensight-airflow:py312-check .`
Expected: build conclui sem erro de resolução de dependências.

- [ ] **Step 2: Subir a stack**

Run: `make dev-airflow`
Expected: containers `postgres`, `airflow-init`, `airflow-webserver` e
`airflow-scheduler` sobem; `airflow-init` termina com sucesso.

- [ ] **Step 3: Disparar a DAG e conferir as 3 tasks**

```bash
docker compose -f docker-compose.airflow.yml exec airflow-webserver \
  airflow dags trigger train_pipeline
```

Expected: em http://localhost:8080 a run aparece com
`load_and_validate_data → train_model → save_model` verdes (3/3).

- [ ] **Step 4: Derrubar a stack**

Run: `docker compose -f docker-compose.airflow.yml down`

- [ ] **Step 5: Decidir**

- **Portão passou:** nada a fazer. Seguir para a Task 3.
- **Portão falhou** (build quebra ou alguma task fica vermelha): reverter
  `Dockerfile.airflow` para `apache/airflow:2.9.3-python3.11` e
  `constraints-3.11.txt`, ajustar
  `tests/test_environment_consistency.py::TestPythonFloor::test_airflow_dockerfile_uses_same_python`
  para aceitar a assimetria (documentando o motivo no docstring do teste), e
  registrar o ocorrido na seção "Consequências" da ADR-0008 (Task 3). O piso
  3.12 do restante do projeto **permanece**.

```bash
# somente no caso de falha
git add Dockerfile.airflow tests/test_environment_consistency.py
git commit -m "fix(airflow): manter imagem em Python 3.11 apos falha do portao 3.12"
```

---

### Task 3: ADR-0008 e correção das divergências pré-existentes do README

A auditoria da spec (§8) encontrou comandos quebrados no README que não têm
relação com esta mudança: a seção do Airflow manda `docker compose up -d
--build`, mas esse compose virou a stack de monitoramento na Etapa 3.

**Files:**
- Create: `docs/ai/adr/0008-piso-unico-de-python.md`
- Modify: `docs/ai/adr/README.md` (tabela de índice)
- Modify: `README.md:9-13` (Status), `README.md:33-41` (Estrutura),
  `README.md:44-53` (pré-requisito de versão), `README.md:135`, `README.md:156`,
  `README.md:180-190`

**Interfaces:**
- Consumes: decisão da Task 2 (Airflow em 3.12 ou 3.11).
- Produces: nada consumido por tarefas seguintes.

- [ ] **Step 1: Escrever a ADR-0008**

Criar `docs/ai/adr/0008-piso-unico-de-python.md`, no formato MADR usado pelas
ADRs existentes:

```markdown
# ADR-0008 — Piso único de Python 3.12

- **Status:** Aceito
- **Data:** 2026-09-02
- **Contexto da decisão:** Etapa 3 (adendo — DVC/uv/Python)

## Contexto

O projeto declarava três pisos diferentes de Python. `pyproject.toml:7` pedia
`>=3.10` e o ruff mirava `py310`; o `Dockerfile` da API e o `ci.yml` usavam
3.12; e `Dockerfile.airflow` fixava `apache/airflow:2.9.3-python3.11`.

O modelo é treinado no container do Airflow e servido pela API. Divergência de
interpretador entre esses dois ambientes é uma das causas clássicas de falha
ao desserializar `models/model.pkl` — a mesma classe de problema que o README
já documenta para `dill` e `scikit-learn`.

Não havia nenhum mecanismo que fizesse essa divergência aparecer: ela só se
manifestaria em runtime.

## Decisão

Piso único `>=3.12` em todo o projeto, incluindo a imagem do Airflow
(`apache/airflow:2.9.3-python3.12` com `constraints-3.12.txt`).

`tests/test_environment_consistency.py` passa a falhar a suíte quando
`pyproject.toml`, `.python-version`, `Dockerfile`, `Dockerfile.airflow` e
`ci.yml` discordam entre si — inclusive quando a URL de constraints deixa de
acompanhar a tag da imagem.

## Alternativas consideradas

**Manter `>=3.10`.** Rejeitada: o piso declarado não descrevia nenhum ambiente
real do projeto, e o README precisava de um aviso de quatro linhas explicando
que o `python3` do sistema podia ser velho demais.

**Manter o Airflow em 3.11 e aceitar a assimetria.** Rejeitada como padrão, mas
mantida como plano B do portão de validação: a tag `2.9.3-python3.12` e o
arquivo `constraints-3.12.txt` foram verificados como existentes antes da
decisão, mas o suporte a 3.12 no Airflow 2.9 é recente.

**Subir o Airflow para 2.10/2.11.** Rejeitada: a DAG já está entregue e vale
15% da nota; trocar a minor do Airflow na reta final da entrega é risco sem
retorno correspondente.

## Consequências

- A imagem do Airflow precisa ser reconstruída (`make dev-airflow --build`).
- `.python-version` passa a ser versionado; quem usa pyenv/uv pega 3.12
  automaticamente.
- `numpy==1.26.4` continua compatível: suporta Python 3.12.
- O teste de coerência precisa ser atualizado de propósito sempre que alguma
  versão mudar — é o ponto, não um efeito colateral.
```

> Se o portão da Task 2 falhou, ajustar a seção "Decisão" para registrar o
> Airflow em 3.11 e mover a alternativa correspondente para lá.

- [ ] **Step 2: Adicionar a ADR-0008 ao índice**

Em `docs/ai/adr/README.md`, acrescentar a linha à tabela:

```markdown
| [0008](0008-piso-unico-de-python.md) | Piso único de Python 3.12 | Aceito |
```

- [ ] **Step 3: Corrigir a seção "Status" do README**

Em `README.md:9-13`, substituir por:

```markdown
## Status

- [x] Etapa 1 — Dataset, modelo baseline e DAG Airflow
- [x] Etapa 2 — API FastAPI, Docker e decisão arquitetural
- [x] Etapa 3 — Testes, CI/CD e observabilidade
- [ ] Etapa 4 — Otimização de latência e benchmark
```

- [ ] **Step 4: Corrigir a "Estrutura do projeto"**

Em `README.md:33-41`, substituir as linhas dos arquivos de topo por:

```
docker-compose.yml         # stack de monitoramento (API + Prometheus + Grafana)
docker-compose.airflow.yml # ambiente local do Airflow (Postgres + webserver + scheduler)
Dockerfile                 # imagem da API de inferencia
Dockerfile.airflow         # imagem do Airflow (nao e a imagem da API)
```

(As linhas de `requirements.txt` / `requirements-dev.txt` são removidas na
Task 6, junto com os arquivos.)

- [ ] **Step 5: Corrigir os comandos quebrados do Airflow**

Em `README.md:135`, trocar por:

```bash
make dev-airflow
# equivalente a: docker compose -f docker-compose.airflow.yml up -d --build
```

Em `README.md:156`, trocar por:

```bash
docker compose -f docker-compose.airflow.yml exec airflow-webserver \
  airflow dags trigger train_pipeline
```

Em `README.md:180-190` (seção "Encerrar o ambiente"), trocar por:

```bash
docker compose -f docker-compose.airflow.yml down

# reset completo do metastore (remove o volume do Postgres)
docker compose -f docker-compose.airflow.yml down -v
```

- [ ] **Step 6: Atualizar o pré-requisito de versão**

Em `README.md:44-53`, substituir "Pré-requisitos: Python 3.10+" e o bloco de
aviso sobre `zip(strict=)` por:

```markdown
Pré-requisitos: Python 3.12+ (o arquivo `.python-version` fixa `3.12`).
```

- [ ] **Step 7: Verificar**

Run: `make test && make lint`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add docs/ai/adr/0008-piso-unico-de-python.md docs/ai/adr/README.md README.md
git commit -m "docs(python): registrar ADR-0008 e corrigir divergencias do README

Corrige comandos quebrados da secao do Airflow (o docker-compose.yml passou
a ser a stack de monitoramento na Etapa 3), atualiza a secao Status e o
pre-requisito de versao de Python."
```

---

# COMMIT 2 — uv como único gerenciador

---

### Task 4: Grupos PEP 735 e pins de compatibilidade de pickle

**Files:**
- Modify: `pyproject.toml:8-27` (dependências e extras)
- Modify: `tests/test_environment_consistency.py` (nova classe)
- Modify: `uv.lock` (relock)

**Interfaces:**
- Consumes: `read_pyproject()`, `PROJECT_ROOT` (Task 1).
- Produces: grupos `pipeline` e `dev` em `[dependency-groups]`, consumidos
  pelas Tasks 5, 10 e 11.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar ao final de `tests/test_environment_consistency.py`:

```python
# Versoes ditadas pelas constraints oficiais do Airflow 2.9.3
# (constraints-3.12.txt). O modelo e treinado sob elas dentro do container
# do Airflow; a API precisa desserializar sob exatamente as mesmas.
AIRFLOW_PINNED = {"numpy": "1.26.4", "pandas": "2.1.4"}


class TestDependencyGroups:
    def test_declares_dependency_groups(self):
        assert "dependency-groups" in read_pyproject()

    def test_has_no_legacy_optional_dependencies(self):
        """Grupos PEP 735 substituem os extras; manter ambos duplica a verdade."""
        assert "optional-dependencies" not in read_pyproject()["project"]

    def test_dev_group_includes_pipeline_group(self):
        dev = read_pyproject()["dependency-groups"]["dev"]
        assert {"include-group": "pipeline"} in dev

    def test_numpy_is_pinned_to_airflow_version(self):
        deps = read_pyproject()["project"]["dependencies"]
        assert f"numpy=={AIRFLOW_PINNED['numpy']}" in deps

    def test_pandas_is_pinned_to_airflow_version_in_pipeline_group(self):
        pipeline = read_pyproject()["dependency-groups"]["pipeline"]
        assert f"pandas=={AIRFLOW_PINNED['pandas']}" in pipeline

    def test_api_runtime_excludes_pipeline_only_packages(self):
        """src/app.py nao importa pandas nem requests: eles nao vao para a imagem."""
        deps = " ".join(read_pyproject()["project"]["dependencies"])
        assert "pandas" not in deps
        assert "requests" not in deps

    def test_dvc_is_not_a_runtime_dependency(self):
        deps = " ".join(read_pyproject()["project"]["dependencies"])
        assert "dvc" not in deps


class TestLockfileMatchesPins:
    def test_lock_resolves_pinned_versions(self):
        """O lock e a fonte de verdade real; os pins precisam ter chegado nele."""
        lock = read_text("uv.lock")
        for package, version in AIRFLOW_PINNED.items():
            assert f'name = "{package}"\nversion = "{version}"' in lock
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v -k "Dependency or Lockfile"`
Expected: FAIL — não existe `[dependency-groups]`, e o lock traz
`numpy 2.2.6` / `pandas 2.3.3`.

- [ ] **Step 3: Reescrever as dependências em `pyproject.toml`**

Substituir os blocos `dependencies` e `[project.optional-dependencies]` por:

```toml
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.8.0",
    "prometheus-client>=0.20",
    # Necessarios para desserializar models/model.pkl na API.
    # numpy fica fixado na versao das constraints do Airflow: o modelo e
    # treinado la e carregado aqui (ver ADR-0007).
    "scikit-learn==1.5.1",
    "joblib>=1.4.0",
    "numpy==1.26.4",
    "dill>=0.3.8",
]

[dependency-groups]
# Preparacao de dados e treino: src/prepare_dataset.py, src/train.py e o
# container do Airflow. Nao entra na imagem da API.
pipeline = [
    "pandas==2.1.4",
    "requests>=2.31.0",
    "dvc>=3.67",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.6.0",
    "httpx>=0.27.0",
    { include-group = "pipeline" },
]
```

- [ ] **Step 4: Relock e sincronizar**

```bash
uv lock && uv sync
```

Se o resolvedor recusar `numpy==1.26.4` por causa de alguma transitiva
(candidato mais provável: `scipy`), fixar também `scipy` na maior versão que
aceite `numpy 1.26` e registrar isso na ADR-0007 (Task 6).

- [ ] **Step 5: Rodar os testes e verificar que passam**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v`
Expected: PASS (todos)

- [ ] **Step 6: Rodar a suíte completa**

Run: `make test && make lint`
Expected: PASS. Atenção especial a `tests/test_model_loading.py` — o
`models/model.pkl` local foi treinado sob `numpy 2.x`; após o downgrade ele
pode emitir aviso ou falhar. Se falhar, regenerar com `make train` e seguir.

- [ ] **Step 7: Commit**

```bash
git add pyproject.toml uv.lock tests/test_environment_consistency.py
git commit -m "refactor(deps): adotar grupos PEP 735 e fixar numpy/pandas do Airflow

Separa runtime da API (sem pandas/requests, que src/app.py nao importa) dos
grupos pipeline e dev. Fixa numpy==1.26.4 e pandas==2.1.4, versoes das
constraints oficiais do Airflow 2.9.3, garantindo que o pickle treinado no
container carregue na API."
```

---

### Task 5: Migrar CI, Docker e Makefile para uv

Esta tarefa é atômica de propósito: deletar os `requirements*.txt` sem migrar
os consumidores deixaria o repositório quebrado.

**Files:**
- Delete: `requirements.txt`, `requirements-dev.txt`
- Modify: `.github/workflows/ci.yml` (jobs `lint`, `test`, `smoke-train`)
- Modify: `Dockerfile:17-20`
- Modify: `Dockerfile.airflow:6-11`
- Modify: `Makefile:1-11`
- Modify: `tests/test_environment_consistency.py` (nova classe)

**Interfaces:**
- Consumes: grupos `pipeline` e `dev` (Task 4).
- Produces: nenhum símbolo Python; o `Makefile` passa a exportar
  `PYTHON = uv run python` incondicionalmente.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_environment_consistency.py`:

```python
class TestSingleSourceOfDependencies:
    def test_requirements_files_are_gone(self):
        """Arquivo commitado que ninguem edita a mao vira arquivo desatualizado."""
        assert not (PROJECT_ROOT / "requirements.txt").exists()
        assert not (PROJECT_ROOT / "requirements-dev.txt").exists()

    def test_ci_installs_with_uv(self):
        ci = read_text(".github/workflows/ci.yml")
        assert "astral-sh/setup-uv" in ci
        assert "pip install" not in ci

    def test_api_dockerfile_installs_with_uv(self):
        dockerfile = read_text("Dockerfile")
        assert "uv sync --frozen --no-default-groups" in dockerfile
        assert "pip install" not in dockerfile

    def test_airflow_dockerfile_installs_pipeline_group(self):
        dockerfile = read_text("Dockerfile.airflow")
        assert "--group pipeline" in dockerfile

    def test_makefile_requires_uv_without_fallback(self):
        makefile = read_text("Makefile")
        assert "uv run python" in makefile
        assert "python3" not in makefile
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v -k SingleSource`
Expected: FAIL em todas as cinco.

- [ ] **Step 3: Atualizar `.github/workflows/ci.yml`**

Nos jobs `lint` e `test`, substituir os blocos `actions/setup-python` +
`pip install` por:

```yaml
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Instalar dependencias
        run: uv sync --frozen
```

O job `lint` passa a rodar `uv run ruff check src/ tests/ dags/`, e o `test`,
`uv run pytest -v`.

No job `smoke-train`, o mesmo setup, mas instalando só o necessário:

```yaml
      - name: Instalar dependencias do pipeline
        run: uv sync --frozen --no-default-groups --group pipeline
      - name: Treinar modelo em amostra minima
        run: |
          mkdir -p models
          uv run python -m src.train \
            --train-path tests/fixtures/sample_dataset.csv \
            --test-path tests/fixtures/sample_dataset.csv \
            --model-out models/model.pkl \
            --metrics-out /tmp/smoke_metrics.md \
            --max-features 500
```

Remover o bloco `env: PYTHON_VERSION` **não**: ele continua sendo lido pelo
teste de coerência. Passá-lo ao uv:

```yaml
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          python-version: ${{ env.PYTHON_VERSION }}
```

- [ ] **Step 4: Atualizar o `Dockerfile` da API**

Substituir as linhas 17-20 por:

```dockerfile
# uv: instalador unico do projeto (ver ADR-0007). Versao fixada para que o
# build seja reproduzivel.
COPY --from=ghcr.io/astral-sh/uv:0.11.1 /uv /bin/uv

# Dependencias primeiro: aproveita o cache de camadas (o layer so e refeito
# quando pyproject.toml ou uv.lock mudam). --no-default-groups exclui dev e
# pipeline: a API nao usa pandas, requests, pytest nem dvc.
COPY pyproject.toml uv.lock ./
ENV UV_PROJECT_ENVIRONMENT=/usr/local
RUN uv sync --frozen --no-default-groups --no-cache
```

- [ ] **Step 5: Atualizar o `Dockerfile.airflow`**

Substituir as linhas 6-11 por:

```dockerfile
COPY --from=ghcr.io/astral-sh/uv:0.11.1 /uv /bin/uv

COPY pyproject.toml uv.lock /opt/urgensight/

# O lock e a fonte de verdade das versoes; as constraints oficiais do Airflow
# continuam protegendo as dependencias transitivas do proprio Airflow (elas
# nao listam scipy, scikit-learn nem joblib -- esses vem do lock).
RUN uv export --frozen --no-default-groups --group pipeline \
        --no-emit-project --project /opt/urgensight \
        -o /tmp/requirements-pipeline.txt \
    && uv pip install --no-cache \
        --python "$(command -v python)" \
        -r /tmp/requirements-pipeline.txt \
        --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.3/constraints-3.12.txt"
```

> **Risco conhecido:** a imagem oficial do Airflow roda como usuário
> `airflow`, com os pacotes em `/home/airflow/.local`. Se
> `uv pip install --python "$(command -v python)"` falhar por permissão,
> tentar `--system`; se ainda falhar, manter `pip install` **apenas nesta
> imagem** e registrar a exceção nas "Consequências" da ADR-0007. Não
> bloquear o commit por isso.

- [ ] **Step 6: Atualizar o `Makefile`**

Substituir as linhas 1-11 por:

```makefile
.PHONY: help dev ensure-model dev-airflow run train test test-cov lint format clean graphify setup-hooks pull-memory push-memory load monitoring-down

# uv e obrigatorio (ADR-0007): o fallback anterior para python3/python do
# sistema permitia que cada integrante rodasse num ambiente diferente.
UV := $(shell command -v uv 2>/dev/null)
ifeq ($(strip $(UV)),)
  $(error uv nao encontrado no PATH. Instale com: curl -LsSf https://astral.sh/uv/install.sh | sh)
endif
PYTHON := uv run python
```

- [ ] **Step 7: Remover os `requirements*.txt`**

```bash
git rm requirements.txt requirements-dev.txt
```

- [ ] **Step 8: Rodar os testes e verificar que passam**

Run: `make test && make lint`
Expected: PASS

- [ ] **Step 9: Validar os dois builds Docker**

```bash
docker build -t urgensight-api:uv-check .
docker run -d -p 8000:8000 --name uv-check urgensight-api:uv-check
sleep 5 && curl -sf http://localhost:8000/health && echo " OK"
docker rm -f uv-check
docker build -f Dockerfile.airflow -t urgensight-airflow:uv-check .
```

Expected: `/health` responde 200. **Se responder 503**, o risco R2 da spec se
materializou: a API precisa de `pandas` para desserializar o pickle. Nesse
caso, mover `pandas==2.1.4` de `pipeline` para `[project.dependencies]`,
ajustar `test_api_runtime_excludes_pipeline_only_packages` (removendo a
asserção de `pandas` e mantendo a de `requests`), relockar e registrar na
ADR-0007.

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "build(deps): substituir pip por uv em CI, Docker e Makefile

Remove requirements.txt e requirements-dev.txt: pyproject.toml + uv.lock
passam a ser a unica fonte de verdade. A imagem da API instala apenas o
runtime; a do Airflow instala o grupo pipeline, mantendo as constraints
oficiais para as transitivas do proprio Airflow."
```

---

### Task 6: ADR-0007 e documentação do uv

**Files:**
- Create: `docs/ai/adr/0007-uv-com-lock-unico.md`
- Modify: `docs/ai/adr/README.md`, `docs/ai/adr/0003-artefato-de-modelo-no-ci.md`
- Modify: `README.md` (seções "Instalação", "Rodando os testes", "Estrutura do
  projeto", nota de consistência de versões em `README.md:166-178`, e o passo 2
  da seção de build em `README.md:215`)
- Modify: `CLAUDE.md` (seção Environment Isolation), `GEMINI.md:19`
- Modify: `docs/api_contract.md:159`, `.dockerignore:2-3`

**Interfaces:**
- Consumes: decisões e eventuais exceções das Tasks 4 e 5.
- Produces: nada consumido adiante.

- [ ] **Step 1: Escrever a ADR-0007**

Criar `docs/ai/adr/0007-uv-com-lock-unico.md`:

```markdown
# ADR-0007 — uv com lock único e grupos PEP 735

- **Status:** Aceito
- **Data:** 2026-09-02
- **Contexto da decisão:** Etapa 3 (adendo — DVC/uv/Python)

## Contexto

O projeto declarava dependências em quatro lugares: `pyproject.toml`,
`requirements.txt`, `requirements-dev.txt` e `uv.lock`. O `uv.lock` estava
commitado (498 KB) e **nenhum ambiente o usava** — CI, `Dockerfile` e
`Dockerfile.airflow` instalavam com `pip` a partir dos `requirements*.txt`.

Os comentários em `requirements-dev.txt` documentavam a cicatriz disso: o
pacote inexistente `httpx2` e a ausência de `ruff` passaram despercebidos
localmente e só quebraram no CI.

Mais grave, medido durante o desenho: `numpy` era resolvido como `1.26.4` no
container do Airflow (constraints oficiais), `1.26.x` na API (teto
`<2.0` em `requirements.txt`) e `2.2.6` no `uv.lock`. O modelo é treinado no
primeiro ambiente e desserializado no segundo. E `scipy`, `scikit-learn`,
`joblib` e `threadpoolctl` — usados pelo `TfidfVectorizer` para serializar
matrizes esparsas — **não constam** das constraints do Airflow, ficando livres
em cada ambiente.

## Decisão

`pyproject.toml` + `uv.lock` são a única fonte de verdade. Os
`requirements*.txt` foram removidos. Dependências são declaradas em três
conjuntos, seguindo os imports reais:

- `[project.dependencies]` — runtime da API. `src/app.py` não importa `pandas`
  nem `requests`; ambos saem da imagem.
- Grupo `pipeline` — `pandas`, `requests`, `dvc`: `src/prepare_dataset.py`,
  `src/train.py` e o container do Airflow.
- Grupo `dev` — ferramentas de teste e lint, incluindo `pipeline`.

`numpy==1.26.4` e `pandas==2.1.4` ficam fixados na versão das constraints do
Airflow: o ambiente menos flexível dita o piso. `scipy`, `scikit-learn` e
`joblib` passam a vir do lock, que é onde deveriam estar.

O `uv` torna-se pré-requisito; o `Makefile` perde o fallback para
`python3`/`python` do sistema.

## Alternativas consideradas

**Manter o pip e os `requirements*.txt`.** Rejeitada: é o estado que produziu
o bug do `httpx2` e a divergência de `numpy`.

**uv no desenvolvimento, exportando `requirements.txt` para Docker/CI.**
Rejeitada: um arquivo gerado e commitado que ninguém edita à mão é exatamente
o que desatualiza sem ninguém perceber.

**Fixar apenas os pacotes que atravessam a fronteira do pickle.** Rejeitada:
reintroduz "dois ambientes que quase batem", que é a origem do problema.

## Consequências

- Todos os integrantes precisam instalar o `uv`; o README passa a instruir
  `uv sync` no lugar de `python -m venv` + `pip install`.
- O projeto fica em `pandas 2.1.4` e `numpy 1.26.4`, mais antigos que o
  disponível. Preço aceito por um único ambiente resolvido.
- A imagem da API encolhe: `pandas` e `requests` deixam de ser instalados.
- Afeta a **ADR-0003**: o job `smoke-train` passa a instalar com
  `uv sync --frozen --no-default-groups --group pipeline`. A decisão daquela
  ADR (dummy training sobre fixture versionado, sem rede) permanece válida.
```

> Se as exceções previstas nas Tasks 4 ou 5 ocorrerem (`scipy` fixado,
> `pandas` de volta ao runtime, `pip` mantido na imagem do Airflow),
> acrescentá-las a "Consequências" **antes** de commitar.

- [ ] **Step 2: Registrar o vínculo na ADR-0003**

Ao final de `docs/ai/adr/0003-artefato-de-modelo-no-ci.md`, acrescentar:

```markdown
> **Atualização (2026-09-02, [ADR-0007](0007-uv-com-lock-unico.md)):** o job
> `smoke-train` passou a instalar dependências com
> `uv sync --frozen --no-default-groups --group pipeline` em vez de
> `pip install -r requirements.txt`. A decisão registrada aqui — dummy
> training sobre fixture versionado, sem rede — permanece em vigor.
```

- [ ] **Step 3: Atualizar o índice de ADRs**

```markdown
| [0007](0007-uv-com-lock-unico.md) | uv com lock único e grupos PEP 735 | Aceito |
```

- [ ] **Step 4: Reescrever a seção "Instalação" do README**

```markdown
## Instalação

Pré-requisitos: Python 3.12+ e [uv](https://docs.astral.sh/uv/).

```bash
# instalar o uv (uma vez)
curl -LsSf https://astral.sh/uv/install.sh | sh

# criar o ambiente e instalar tudo (dev + pipeline)
uv sync
```

`uv sync` lê `pyproject.toml` e `uv.lock`, cria o `.venv` e instala as versões
exatas do lock. Não é necessário ativar o ambiente: `uv run <comando>` e os
alvos do `Makefile` já executam dentro dele.

O projeto declara três conjuntos de dependências:

| Conjunto | Conteúdo | Instalação |
|---|---|---|
| `[project.dependencies]` | runtime da API | `uv sync --no-default-groups` |
| grupo `pipeline` | `pandas`, `requests`, `dvc` | `uv sync --no-default-groups --group pipeline` |
| grupo `dev` (padrão) | pytest, ruff, httpx + `pipeline` | `uv sync` |
```

- [ ] **Step 5: Atualizar as demais referências a `requirements.txt`**

| Arquivo | Trecho | Substituir por |
|---|---|---|
| `README.md` (seção "Rodando os testes") | `pytest -q` | `make test` |
| `README.md:166-178` (nota de consistência) | "declarado em `requirements.txt`" | "declarado em `pyproject.toml` e fixado em `uv.lock`"; acrescentar `numpy==1.26.4` à lista de pacotes fixados e citar a ADR-0007 |
| `README.md:215` (passo 2 do build) | "Instala `requirements.txt` em uma camada própria" | "Instala as dependências de runtime com `uv sync --frozen --no-default-groups` em uma camada própria" |
| `README.md:33-41` | linhas de `requirements*.txt` | `pyproject.toml  # dependencias, grupos e configuracao de ruff/pytest` e `uv.lock  # versoes exatas (fonte de verdade)` |
| `CLAUDE.md` (Environment Isolation) | "`dill` e `scikit-learn==1.5.1` ensure serialized pickle compatibility" | acrescentar `numpy==1.26.4` e trocar a menção a pip/requirements por `uv sync` |
| `GEMINI.md:19` | "fixados em `requirements.txt`" | "fixados em `pyproject.toml`/`uv.lock`" |
| `docs/api_contract.md:159` | "compativel com as versoes de requirements.txt" | "compativel com as versoes de pyproject.toml/uv.lock" |
| `.dockerignore:2-3` | "copia apenas src/, requirements.txt e models/model.pkl" | "copia apenas src/, pyproject.toml, uv.lock e models/model.pkl" |

- [ ] **Step 6: Verificar que não sobrou referência órfã**

```bash
grep -rniE "requirements(-dev)?\.txt|pip install" README.md CLAUDE.md GEMINI.md \
  docs/api_contract.md docs/dataset.md .dockerignore Makefile Dockerfile* \
  .github/workflows/ci.yml
```

Expected: nenhuma ocorrência, exceto o comando de instalação do próprio `uv`
e as menções históricas dentro de `docs/ai/adr/` e `docs/ai/plans/`.

- [ ] **Step 7: Verificar**

Run: `make test && make lint`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "docs(deps): registrar ADR-0007 e atualizar documentacao para uv

Reescreve a instalacao do README para uv sync, atualiza as referencias a
requirements.txt em CLAUDE.md, GEMINI.md, api_contract.md e .dockerignore, e
acrescenta numpy a nota de consistencia de versoes entre ambientes."
```

---

# COMMIT 3 — DVC (revertível)

> **Gate de aborto (spec §6.1):** este bloco inteiro é revertido se, ao final,
> `make test`, o CI ou a DAG local não estiverem verdes, **ou** se exigir
> alterar `docker-compose.airflow.yml` ou quebrar testes que hoje passam.
> Reverter aqui não desfaz os Commits 1 e 2.

---

### Task 7: Separar download e preparação em `src/prepare_dataset.py`

**Files:**
- Modify: `src/prepare_dataset.py:167-176` (`parse_args`), `:178-230` (`main`)
- Modify: `tests/test_prepare_dataset.py`

**Interfaces:**
- Consumes: nada das tarefas anteriores.
- Produces: `parse_args(argv) -> Namespace` com os atributos booleanos novos
  `download_only` e `skip_download`. `main(argv)` respeita ambos. Consumido
  pelo `dvc.yaml` (Task 9).

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_prepare_dataset.py`:

```python
from src.prepare_dataset import main, parse_args


class TestStageFlags:
    def test_parse_args_defaults_to_full_run(self):
        args = parse_args([])
        assert args.download_only is False
        assert args.skip_download is False

    def test_parse_args_accepts_download_only(self):
        assert parse_args(["--download-only"]).download_only is True

    def test_parse_args_accepts_skip_download(self):
        assert parse_args(["--skip-download"]).skip_download is True

    def test_flags_are_mutually_exclusive(self):
        with pytest.raises(SystemExit):
            parse_args(["--download-only", "--skip-download"])

    def test_download_only_stops_before_processing(self, tmp_path, monkeypatch):
        """--download-only baixa e para: nao gera data/processed."""
        calls = {}

        def fake_download(dest_dir, force=False):
            calls["download"] = True
            dest_dir.mkdir(parents=True, exist_ok=True)
            return dest_dir / "train.csv", dest_dir / "test.csv"

        def fail_load(*_args, **_kwargs):
            raise AssertionError("load_raw_data nao deveria ser chamado")

        monkeypatch.setattr("src.prepare_dataset.download_raw_data", fake_download)
        monkeypatch.setattr("src.prepare_dataset.load_raw_data", fail_load)

        main(["--download-only", "--data-dir", str(tmp_path)])

        assert calls["download"] is True
        assert not (tmp_path / "processed" / "train.csv").exists()

    def test_skip_download_does_not_hit_the_network(self, tmp_path, monkeypatch):
        """--skip-download consome data/raw ja existente."""

        def fail_download(*_args, **_kwargs):
            raise AssertionError("download_raw_data nao deveria ser chamado")

        monkeypatch.setattr("src.prepare_dataset.download_raw_data", fail_download)

        raw_dir = tmp_path / "raw"
        raw_dir.mkdir(parents=True)
        raw_df = make_raw_df().dropna()
        raw_df.to_csv(raw_dir / "medical_tc_train.csv", index=False)
        raw_df.to_csv(raw_dir / "medical_tc_test.csv", index=False)

        main(["--skip-download", "--data-dir", str(tmp_path)])

        assert (tmp_path / "processed" / "train.csv").exists()
        assert (tmp_path / "processed" / "test.csv").exists()
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `uv run python -m pytest tests/test_prepare_dataset.py -v -k StageFlags`
Expected: FAIL — `AttributeError: 'Namespace' object has no attribute 'download_only'`

- [ ] **Step 3: Implementar as flags**

Em `parse_args`, antes do `return`:

```python
    # Flags de estagio: o pipeline DVC separa o download (dependente de rede)
    # do processamento (deterministico), para que mudar um parametro de split
    # nao rebaixe os 17 MB de data/raw. Ver dvc.yaml.
    stage_group = parser.add_mutually_exclusive_group()
    stage_group.add_argument("--download-only", action="store_true")
    stage_group.add_argument("--skip-download", action="store_true")
```

Em `main`, substituir o bloco de download por:

```python
    if args.skip_download:
        train_path = raw_dir / "medical_tc_train.csv"
        test_path = raw_dir / "medical_tc_test.csv"
        for path in (train_path, test_path):
            if not path.exists():
                raise FileNotFoundError(
                    f"Arquivo bruto ausente: {path}. "
                    "Rode o estagio de download antes (--download-only)."
                )
    else:
        print(f"Baixando dataset bruto em {raw_dir} ...")
        train_path, test_path = download_raw_data(raw_dir, force=args.force_download)

    if args.download_only:
        print("Download concluido (--download-only): nada mais a fazer.")
        return
```

- [ ] **Step 4: Rodar os testes e verificar que passam**

Run: `uv run python -m pytest tests/test_prepare_dataset.py -v`
Expected: PASS (todos, incluindo os pré-existentes)

- [ ] **Step 5: Commit**

```bash
git add src/prepare_dataset.py tests/test_prepare_dataset.py
git commit -m "feat(dataset): separar download e processamento em estagios

Adiciona --download-only e --skip-download (mutuamente exclusivas) para que
o pipeline DVC trate o download, que depende de rede, como estagio proprio."
```

---

### Task 8: Emitir métricas em JSON

**Files:**
- Modify: `src/train.py:249-260` (`parse_args`), `:262-296` (`main`)
- Create (função): `format_metrics_json` em `src/train.py`
- Modify: `tests/test_train.py`

**Interfaces:**
- Consumes: `evaluate_pipeline(pipeline, X_test, y_test) -> dict` (existente).
- Produces: `format_metrics_json(metrics: dict, config: dict) -> dict` — chaves
  de topo `accuracy`, `macro_f1`, `weighted_f1`, `model`, `seed`, e `f1_<classe>`
  por classe. `parse_args` ganha `--metrics-json-out` (default
  `docs/model_metrics.json`). Consumido pelo `dvc.yaml` (Task 9).

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_train.py`:

```python
import json

from src.train import format_metrics_json, main


class TestFormatMetricsJson:
    def _metrics(self):
        df = make_dataset(n_per_class=10)
        X, y = df[TEXT_COLUMN], df[TARGET_COLUMN]
        pipeline = build_pipeline(model_name="logreg", seed=42).fit(X, y)
        return evaluate_pipeline(pipeline, X, y)

    def test_returns_flat_scalar_mapping(self):
        """dvc metrics show espera escalares, nao estruturas aninhadas."""
        payload = format_metrics_json(
            self._metrics(), config={"model_name": "logreg", "seed": 42}
        )
        for key, value in payload.items():
            assert isinstance(value, (int, float, str)), f"{key} nao e escalar"

    def test_contains_headline_metrics(self):
        payload = format_metrics_json(
            self._metrics(), config={"model_name": "logreg", "seed": 42}
        )
        assert "accuracy" in payload
        assert "macro_f1" in payload
        assert payload["model"] == "logreg"
        assert payload["seed"] == 42

    def test_contains_per_class_f1(self):
        payload = format_metrics_json(
            self._metrics(), config={"model_name": "logreg", "seed": 42}
        )
        for target in VALID_TARGETS:
            assert f"f1_{target}" in payload

    def test_is_json_serializable(self):
        payload = format_metrics_json(
            self._metrics(), config={"model_name": "logreg", "seed": 42}
        )
        assert json.loads(json.dumps(payload)) == payload


class TestMainWritesBothMetricFiles:
    def test_writes_markdown_and_json(self, tmp_path):
        df = make_dataset(n_per_class=10)
        data_path = tmp_path / "data.csv"
        df.to_csv(data_path, index=False)

        md_out = tmp_path / "metrics.md"
        json_out = tmp_path / "metrics.json"

        main([
            "--train-path", str(data_path),
            "--test-path", str(data_path),
            "--model-out", str(tmp_path / "model.pkl"),
            "--metrics-out", str(md_out),
            "--metrics-json-out", str(json_out),
            "--max-features", "50",
        ])

        assert md_out.exists()
        payload = json.loads(json_out.read_text("utf-8"))
        assert 0.0 <= payload["accuracy"] <= 1.0
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `uv run python -m pytest tests/test_train.py -v -k "MetricsJson or BothMetric"`
Expected: FAIL — `ImportError: cannot import name 'format_metrics_json'`

- [ ] **Step 3: Implementar**

Adicionar a `src/train.py`, após `format_metrics_report`:

```python
def format_metrics_json(metrics: dict, config: dict) -> dict:
    """Achata as metricas em escalares para `dvc metrics show/diff`.

    O relatorio Markdown continua sendo a evidencia legivel da entrega; este
    JSON existe para que o DVC consiga comparar runs entre commits.
    """
    payload = {
        "model": config["model_name"],
        "seed": config["seed"],
        "accuracy": round(float(metrics["accuracy"]), 4),
        "macro_f1": round(float(metrics["macro_f1"]), 4),
        "weighted_f1": round(float(metrics["weighted_f1"]), 4),
    }
    for target in sorted(VALID_TARGETS):
        payload[f"f1_{target}"] = round(
            float(metrics["per_class"][target]["f1-score"]), 4
        )
    return payload
```

Em `parse_args`, junto aos outros caminhos:

```python
    parser.add_argument(
        "--metrics-json-out", type=Path, default=Path("docs/model_metrics.json")
    )
```

Em `main`, logo após a escrita do relatório Markdown:

```python
    payload = format_metrics_json(
        metrics, config={"model_name": args.model, "seed": args.seed}
    )
    args.metrics_json_out.parent.mkdir(parents=True, exist_ok=True)
    args.metrics_json_out.write_text(
        json.dumps(payload, indent=2) + "\n", encoding="utf-8"
    )
    print(f"Metricas (JSON) salvas em {args.metrics_json_out}")
```

Adicionar `import json` ao topo do módulo.

- [ ] **Step 4: Rodar os testes e verificar que passam**

Run: `uv run python -m pytest tests/test_train.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/train.py tests/test_train.py
git commit -m "feat(metrics): emitir docs/model_metrics.json alem do relatorio

O Markdown continua sendo a evidencia legivel da entrega; o JSON com
escalares permite dvc metrics show/diff entre commits."
```

---

### Task 9: Inicializar o DVC e declarar o pipeline

**Files:**
- Create: `params.yaml`, `dvc.yaml`, `.dvcignore`, `.dvc/config` (via `dvc init`)
- Modify: `.gitignore`, `.dockerignore`
- Modify: `tests/test_environment_consistency.py` (nova classe)

**Interfaces:**
- Consumes: `--download-only` / `--skip-download` (Task 7),
  `--metrics-json-out` (Task 8).
- Produces: os stages `download`, `prepare` e `train`, invocados por
  `dvc repro <stage>` na Task 10.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_environment_consistency.py`:

```python
class TestDvcPipeline:
    """O params.yaml e a fonte unica dos hiperparametros (ADR-0006).

    Antes do DVC, seed e max_features viviam duplicados em src/train.py
    (argparse), dags/train_pipeline.py (hardcoded) e ci.yml.
    """

    def _params(self) -> dict:
        import yaml

        return yaml.safe_load(read_text("params.yaml"))

    def _dvc_yaml(self) -> dict:
        import yaml

        return yaml.safe_load(read_text("dvc.yaml"))

    def test_params_declares_prepare_and_train(self):
        params = self._params()
        assert set(params["prepare"]) == {"seed", "test_size", "benchmark_per_class"}
        assert set(params["train"]) == {"model", "seed", "max_features"}

    def test_pipeline_has_three_stages_in_order(self):
        assert list(self._dvc_yaml()["stages"]) == ["download", "prepare", "train"]

    def test_each_stage_declares_its_params(self):
        stages = self._dvc_yaml()["stages"]
        assert stages["prepare"]["params"] == ["prepare"]
        assert stages["train"]["params"] == ["train"]

    def test_dag_does_not_hardcode_hyperparameters(self):
        """A DAG delega ao DVC; nao pode reintroduzir seed/modelo literais."""
        dag = read_text("dags/train_pipeline.py")
        assert "seed = 42" not in dag
        assert 'model_name="logreg"' not in dag

    def test_dvcstore_is_gitignored(self):
        assert ".dvcstore/" in read_text(".gitignore")
```

- [ ] **Step 2: Rodar e verificar que falha**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v -k DvcPipeline`
Expected: FAIL — `params.yaml` não existe.

> `test_dag_does_not_hardcode_hyperparameters` só passa após a Task 10. É
> esperado que ele fique vermelho ao fim desta tarefa; **não commitar a Task 9
> sozinha** — os passos abaixo já preveem isso no Step 8.

- [ ] **Step 3: Inicializar o DVC**

```bash
uv run dvc init
uv run dvc remote add -d local .dvcstore
```

- [ ] **Step 4: Criar `params.yaml`**

```yaml
# Fonte unica dos hiperparametros do pipeline (ADR-0006).
# O dvc.yaml declara quais campos cada estagio consome; mudar um valor aqui
# invalida somente os estagios que dependem dele.
#
# Para uma variacao pontual sem editar este arquivo:
#   dvc exp run -S train.max_features=500

prepare:
  seed: 42
  test_size: 0.2
  benchmark_per_class: 15

train:
  model: logreg
  seed: 42
  max_features: 20000
```

- [ ] **Step 5: Criar `dvc.yaml`**

```yaml
stages:
  download:
    cmd: python -m src.prepare_dataset --download-only --data-dir data
    deps:
      - src/prepare_dataset.py
    outs:
      - data/raw

  prepare:
    cmd: >-
      python -m src.prepare_dataset --skip-download --data-dir data
      --seed ${prepare.seed}
      --test-size ${prepare.test_size}
      --benchmark-per-class ${prepare.benchmark_per_class}
    deps:
      - src/prepare_dataset.py
      - data/raw
    params:
      - prepare
    outs:
      - data/processed
      - data/benchmark_samples.json:
          cache: false
      - docs/dataset_distribution.md:
          cache: false

  train:
    cmd: >-
      python -m src.train --model ${train.model}
      --seed ${train.seed}
      --max-features ${train.max_features}
    deps:
      - src/train.py
      - src/prepare_dataset.py
      - data/processed
    params:
      - train
    outs:
      - models/model.pkl
      - docs/model_metrics.md:
          cache: false
    metrics:
      - docs/model_metrics.json:
          cache: false
```

`cache: false` mantém no Git os artefatos que já eram versionados
(`benchmark_samples.json`) e as evidências de entrega em `docs/`.

- [ ] **Step 6: Atualizar `.gitignore` e `.dockerignore`**

Em `.gitignore`, na seção de dados:

```
# DVC: remote local e cache (os metadados .dvc/ e dvc.lock vao para o Git)
.dvcstore/
/.dvc/cache
/.dvc/tmp
```

Em `.dockerignore`:

```
# DVC (a imagem da API recebe o model.pkl pronto, nao reproduz o pipeline)
.dvcstore/
.dvc/
dvc.yaml
dvc.lock
params.yaml
```

- [ ] **Step 7: Reproduzir o pipeline e validar o cache seletivo**

```bash
uv run dvc repro                       # roda os 3 estagios
uv run dvc repro                       # deve reportar tudo "up to date"
uv run dvc push                        # popula .dvcstore/
```

Expected: a segunda execução não reexecuta nada.

Validar o comportamento que justifica o DVC:

```bash
uv run dvc exp run -S train.max_features=500 --dry
```

Expected: o plano indica que apenas `train` seria reexecutado — `download` e
`prepare` permanecem em cache.

- [ ] **Step 8: Verificar e seguir para a Task 10 sem commitar**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v -k DvcPipeline`
Expected: todos PASS, **exceto**
`test_dag_does_not_hardcode_hyperparameters`, que depende da Task 10.

Não commitar aqui. O commit acontece ao final da Task 10, com a DAG já migrada.

---

### Task 10: DAG delegando ao DVC

**Files:**
- Modify: `dags/train_pipeline.py:47-118` (corpo das três tasks)
- Modify: `Dockerfile.airflow` (instalar `git` e configurar `safe.directory`)

**Interfaces:**
- Consumes: stages `prepare` e `train` (Task 9).
- Produces: nenhum símbolo Python novo. Os três `task_id`
  (`load_and_validate_data`, `train_model`, `save_model`) são **preservados** —
  `tests/test_train_pipeline_dag.py` continua passando sem edição.

- [ ] **Step 1: Rodar os testes da DAG antes de mexer (baseline)**

Run: `uv run python -m pytest tests/test_train_pipeline_dag.py -v`
Expected: PASS ou SKIP (skip é esperado fora do container — o `airflow` não
está instalado localmente). Anotar qual dos dois ocorreu.

- [ ] **Step 2: Substituir o corpo das tasks**

Em `dags/train_pipeline.py`, trocar as três funções de task por:

```python
    def _run_dvc(*args: str) -> None:
        """Executa um comando dvc na raiz do projeto, propagando a falha."""
        import subprocess

        result = subprocess.run(
            ["dvc", *args],
            cwd=PROJECT_DIR,
            capture_output=True,
            text=True,
        )
        print(result.stdout)
        if result.returncode != 0:
            print(result.stderr)
            raise RuntimeError(
                f"dvc {' '.join(args)} falhou (codigo {result.returncode})"
            )

    @task
    def load_and_validate_data() -> dict:
        """Reproduz o estagio de preparacao (arrasta o download se preciso).

        A validacao de colunas continua sendo feita por src/train.py dentro
        do estagio; aqui garantimos que data/processed esta atualizado em
        relacao a data/raw e aos params de prepare.
        """
        _run_dvc("repro", "prepare")

        from src.train import load_dataset, validate_columns

        train_path = DATA_DIR / "processed" / "train.csv"
        test_path = DATA_DIR / "processed" / "test.csv"
        for path in (train_path, test_path):
            validate_columns(load_dataset(path))

        print(f"Dataset validado: {train_path.name}, {test_path.name}")
        return {"train_path": str(train_path), "test_path": str(test_path)}

    @task
    def train_model(data_info: dict) -> dict:
        """Reproduz o estagio de treino. Hiperparametros vem de params.yaml."""
        import json

        _run_dvc("repro", "train")

        metrics_path = DOCS_DIR / "model_metrics.json"
        metrics = json.loads(metrics_path.read_text(encoding="utf-8"))

        print(
            f"Treino concluido: accuracy={metrics['accuracy']:.4f}, "
            f"macro_f1={metrics['macro_f1']:.4f}"
        )
        return {
            "model_path": str(MODELS_DIR / "model.pkl"),
            "accuracy": metrics["accuracy"],
            "macro_f1": metrics["macro_f1"],
        }

    @task
    def save_model(train_result: dict) -> None:
        """Publica o modelo no remote do DVC e guarda o historico local."""
        model_path = Path(train_result["model_path"])
        _run_dvc("push")

        history_dir = MODELS_DIR / "history"
        history_dir.mkdir(parents=True, exist_ok=True)
        timestamp = datetime.now(UTC).strftime("%Y%m%dT%H%M%S")
        shutil.copyfile(model_path, history_dir / f"model_{timestamp}.pkl")

        print(
            f"Modelo publicado no remote e versionado em dvc.lock "
            f"(accuracy={train_result['accuracy']:.4f}, "
            f"macro_f1={train_result['macro_f1']:.4f})"
        )
```

Ajustar os imports do topo: `from datetime import UTC, datetime` (substitui o
`datetime.utcnow()` depreciado). O `staging_path` deixa de existir — o DVC já
trata `models/model.pkl` como output do stage.

- [ ] **Step 3: Instalar `git` e `dvc` na imagem do Airflow**

O DVC exige um repositório Git para funcionar. Acrescentar ao
`Dockerfile.airflow`, antes do bloco de instalação de dependências:

```dockerfile
USER root
RUN apt-get update \
    && apt-get install -y --no-install-recommends git \
    && rm -rf /var/lib/apt/lists/*
USER airflow

# O repo e montado como volume: o uid do container difere do dono dos
# arquivos, e o git recusa operar em diretorio "dubious ownership".
ENV GIT_CONFIG_COUNT=1 \
    GIT_CONFIG_KEY_0=safe.directory \
    GIT_CONFIG_VALUE_0=/opt/airflow/project
```

O pacote `dvc` já entra pelo grupo `pipeline` (Task 5).

- [ ] **Step 4: Rodar os testes da DAG**

Run: `uv run python -m pytest tests/test_train_pipeline_dag.py -v`
Expected: o mesmo resultado do Step 1 (PASS ou SKIP). Se antes passava e agora
falha, **o gate de aborto foi acionado** — ver Task 12, Step 6.

- [ ] **Step 5: Validar a DAG de verdade no container**

```bash
make dev-airflow
docker compose -f docker-compose.airflow.yml exec airflow-webserver \
  airflow dags trigger train_pipeline
```

Expected: 3/3 tasks verdes em http://localhost:8080. Conferir no log de
`train_model` que o DVC reportou os stages em cache quando nada mudou.

```bash
docker compose -f docker-compose.airflow.yml down
```

- [ ] **Step 6: Rodar a suíte completa**

Run: `make test && make lint`
Expected: PASS, incluindo `test_dag_does_not_hardcode_hyperparameters`.

- [ ] **Step 7: Commit (Tasks 9 + 10 juntas)**

```bash
git add .dvc .dvcignore dvc.yaml dvc.lock params.yaml .gitignore .dockerignore \
        dags/train_pipeline.py Dockerfile.airflow \
        tests/test_environment_consistency.py \
        data/benchmark_samples.json docs/dataset_distribution.md \
        docs/model_metrics.md docs/model_metrics.json
git commit -m "feat(dvc): adotar pipeline reprodutivel com params.yaml

Declara os estagios download -> prepare -> train em dvc.yaml, centraliza os
hiperparametros em params.yaml e faz as tasks da DAG chamarem dvc repro. Os
task_id sao preservados: a ordem carregamento -> treino -> salvamento
continua visivel na UI do Airflow."
```

---

### Task 11: Teste de consistência entre `params.yaml` e `dvc.lock`

**Files:**
- Modify: `tests/test_environment_consistency.py` (nova classe)

**Interfaces:**
- Consumes: `read_text()` (Task 1), `dvc.yaml` / `dvc.lock` / `params.yaml`
  (Tasks 9-10).
- Produces: nada consumido adiante.

> **Por que um teste e não um job de CI.** A spec (decisão Q9, risco R5) previa
> um job `dvc-check`, com a ressalva de que talvez não existisse comando limpo
> para validar sem os dados em cache — e não existe: `dvc status` compara
> hashes de dependências ausentes. Mas o `dvc.lock` **registra os valores de
> params usados em cada estágio**, então a checagem é puro parsing de YAML: sem
> rede, sem cache, sem `dvc` instalado no runner. Ela entra na suíte que o CI
> já roda, em vez de um job novo. O CI continua sem rodar `dvc repro`, que
> reintroduziria a dependência de `raw.githubusercontent.com` rejeitada pela
> ADR-0003.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_environment_consistency.py`:

```python
class TestDvcLockIsInSync:
    """O erro humano mais provavel: editar params.yaml e esquecer do lock.

    dvc.lock registra, por estagio, os valores de params que produziram
    aquele output. Se divergirem de params.yaml, o lock commitado descreve
    um modelo que ninguem consegue reproduzir.
    """

    def _yaml(self, relative_path: str) -> dict:
        import yaml

        return yaml.safe_load(read_text(relative_path))

    def test_lock_exists(self):
        assert (PROJECT_ROOT / "dvc.lock").exists(), (
            "dvc.lock ausente: rode `uv run dvc repro` e commite o resultado"
        )

    def test_lock_covers_every_declared_stage(self):
        declared = set(self._yaml("dvc.yaml")["stages"])
        locked = set(self._yaml("dvc.lock")["stages"])
        assert declared == locked

    def test_locked_params_match_params_yaml(self):
        params = self._yaml("params.yaml")
        for stage_name, stage in self._yaml("dvc.lock")["stages"].items():
            locked = stage.get("params", {}).get("params.yaml", {})
            for section, values in locked.items():
                assert values == params[section], (
                    f"estagio '{stage_name}': params.yaml mudou sem relock. "
                    "Rode `uv run dvc repro` e commite o dvc.lock."
                )

    def test_every_stage_with_params_declares_them_in_dvc_yaml(self):
        """Um param nao declarado nao entra no hash: mudar nao reexecuta nada."""
        lock_stages = self._yaml("dvc.lock")["stages"]
        yaml_stages = self._yaml("dvc.yaml")["stages"]
        for stage_name, stage in lock_stages.items():
            if stage.get("params"):
                assert yaml_stages[stage_name].get("params"), (
                    f"estagio '{stage_name}' usa params sem declara-los em dvc.yaml"
                )
```

- [ ] **Step 2: Rodar e verificar que passa com o lock atual**

Run: `uv run python -m pytest tests/test_environment_consistency.py -v -k DvcLock`
Expected: PASS (o `dvc.lock` foi gerado na Task 9 e está sincronizado).

- [ ] **Step 3: Provar que o teste realmente pega o erro**

```bash
# quebrar de proposito
sed -i.bak 's/seed: 42/seed: 43/' params.yaml
uv run python -m pytest tests/test_environment_consistency.py -v -k locked_params
```

Expected: FAIL com "params.yaml mudou sem relock".

```bash
# restaurar
mv params.yaml.bak params.yaml
uv run python -m pytest tests/test_environment_consistency.py -v -k DvcLock
```

Expected: PASS.

- [ ] **Step 4: Garantir que `pyyaml` está disponível**

`dvc` já depende de `pyyaml`, e o grupo `dev` inclui `pipeline` — o import
funciona localmente e no job `test` do CI. Confirmar:

Run: `uv run python -c "import yaml; print(yaml.__version__)"`
Expected: imprime a versão sem erro. Se falhar, adicionar `"pyyaml>=6.0"` ao
grupo `dev` em `pyproject.toml` e relockar.

- [ ] **Step 5: Rodar a suíte completa**

Run: `make test && make lint`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add tests/test_environment_consistency.py pyproject.toml uv.lock
git commit -m "test(dvc): falhar quando params.yaml e dvc.lock divergirem

Compara os params registrados no dvc.lock com params.yaml por parsing de
YAML: sem rede e sem cache, roda na suite que o CI ja executa. O CI continua
sem reproduzir o pipeline, que dependeria de rede externa (ADR-0003)."
```

---

### Task 12: ADR-0006, documentação do DVC e avaliação do gate

**Files:**
- Create: `docs/ai/adr/0006-dvc-como-fonte-de-verdade-do-pipeline.md`
- Modify: `docs/ai/adr/README.md`
- Modify: `README.md` (nova seção "Pipeline de dados com DVC"; "Estrutura do
  projeto"; seções de Etapa 1.1 e 1.2, que hoje mandam rodar os módulos direto)
- Modify: `docs/dataset.md` (obtenção via `dvc pull`)
- Modify: `docs/specs/phase-tracking.md` (Etapa 3)

**Interfaces:**
- Consumes: tudo das Tasks 7-11, incluindo eventuais exceções.
- Produces: nada.

- [ ] **Step 1: Escrever a ADR-0006**

Criar `docs/ai/adr/0006-dvc-como-fonte-de-verdade-do-pipeline.md`:

```markdown
# ADR-0006 — DVC como fonte de verdade do pipeline de treino

- **Status:** Aceito
- **Data:** 2026-09-02
- **Contexto da decisão:** Etapa 3 (adendo — DVC/uv/Python)

## Contexto

Os hiperparâmetros do treino viviam em três lugares que podiam divergir sem
que nada quebrasse: `src/train.py` (defaults do `argparse`),
`dags/train_pipeline.py` (`seed = 42` e `model_name="logreg"` literais) e
`.github/workflows/ci.yml` (`--max-features 500`).

O versionamento de modelos era `shutil.copyfile` para
`models/history/model_<timestamp>.pkl`: registrava **quando** o modelo foi
treinado, mas não com quais dados, código ou parâmetros. Não havia como
responder "por que este modelo classificou assim?".

E `data/raw` (17 MB) só existia porque um repositório de terceiros continuava
no ar — `.gitignore` o descreve como "regenerable via
`src/prepare_dataset.py`", o que depende de `raw.githubusercontent.com`.

DVC não pontua na rubrica da Fase 3; a adoção é uma decisão de engenharia.

## Decisão

`dvc.yaml` declara três estágios — `download` → `prepare` → `train` — e
`params.yaml` é a fonte única dos hiperparâmetros. O `dvc.yaml` injeta os
valores na linha de comando, de modo que `src/` permanece agnóstico ao DVC e
o `argparse` continua sendo a interface pública.

O Airflow continua sendo o orquestrador: cada task chama `dvc repro <stage>`.
Os três `task_id` são preservados, mantendo visível na UI a ordem
"carregamento → treino → salvamento" exigida pela rubrica.

Remote local em `.dvcstore/`, dentro do repositório e gitignored.

## Alternativas consideradas

**Manter a lógica de treino dentro da DAG.** Rejeitada: é o estado que produziu
os três conjuntos divergentes de parâmetros.

**DVC substituindo o Airflow (uma única task `dvc repro`).** Rejeitada:
achataria a DAG e arriscaria o critério de orquestração, que vale 15% e exige
as etapas de ingestão e treino visíveis.

**Perfis nomeados no `params.yaml`** (`profiles.ci` / `profiles.prod`).
Rejeitada: cada estágio passaria a depender de campos que não usa,
comprometendo o cálculo de hash do DVC e tornando ambíguo qual perfil gerou o
`dvc.lock`. Variação pontual usa `dvc exp run -S`.

**Quatro estágios** (`preprocess → feature_eng → train → evaluate`, como no
material da Fase 2). Rejeitada: o TF-IDF é um passo do `Pipeline` do
scikit-learn dentro de `src/train.py`, e separá-lo quebraria a serialização —
custo alto para imitar um diagrama.

**Remote em S3/GCS.** Rejeitada: exigiria credenciais e um secret no CI. O
material da Fase 2 aceita "local ou S3".

## Consequências

- A imagem do Airflow ganha `git` e `dvc`, e precisa de
  `safe.directory` configurado (o repo é um volume com uid diferente).
- `data/raw` deixa de depender de um host de terceiros após o primeiro
  `dvc push`: qualquer integrante faz `dvc pull`.
- `dvc.lock` passa a responder "quais dados + código + parâmetros geraram este
  modelo".
- `models/history/` continua existindo e agora é redundante com o `dvc.lock`;
  removê-lo é decisão separada, adiada para depois da entrega.
- O CI **não** reproduz o pipeline (ver ADR-0003). A consistência entre
  `params.yaml` e `dvc.lock` é verificada por teste da suíte
  (`tests/test_environment_consistency.py`), por parsing de YAML — sem rede,
  sem cache e sem `dvc` instalado no runner.
```

- [ ] **Step 2: Atualizar o índice de ADRs**

```markdown
| [0006](0006-dvc-como-fonte-de-verdade-do-pipeline.md) | DVC como fonte de verdade do pipeline de treino | Aceito |
```

- [ ] **Step 3: Adicionar a seção do DVC ao README**

Inserir antes de "## API de inferência (Etapa 2)":

````markdown
## Pipeline de dados com DVC

O pipeline de dados e treino é declarado em `dvc.yaml`, com três estágios:

```
download  →  prepare  →  train
data/raw     data/processed   models/model.pkl
             benchmark_samples.json   docs/model_metrics.{md,json}
```

Os hiperparâmetros ficam em `params.yaml` — fonte única, consumida tanto pela
execução local quanto pela DAG do Airflow.

### Obter os dados e o modelo

```bash
uv run dvc pull
```

Baixa `data/` e `models/model.pkl` do remote local (`.dvcstore/`) na versão
correspondente ao commit atual, sem depender do repositório de terceiros de
onde o corpus foi originalmente baixado.

### Reproduzir o pipeline

```bash
uv run dvc repro
```

O DVC reexecuta **apenas** os estágios afetados. Alterar
`train.max_features` em `params.yaml` retreina o modelo sem reprocessar os
17 MB de `data/raw`.

### Experimentar sem editar `params.yaml`

```bash
uv run dvc exp run -S train.max_features=500
uv run dvc metrics diff
```

### Publicar artefatos

```bash
uv run dvc push
```
````

- [ ] **Step 4: Atualizar as seções de Etapa 1.1 e 1.2 do README**

Onde hoje instruem `python -m src.prepare_dataset` e `python -m src.train`,
acrescentar que `uv run dvc repro` é o caminho recomendado (reaproveita cache),
mantendo os comandos diretos como alternativa.

Em "Estrutura do projeto", acrescentar:

```
params.yaml   # hiperparametros do pipeline (fonte unica)
dvc.yaml      # estagios: download -> prepare -> train
dvc.lock      # hashes dos dados/modelo de cada execucao
.dvcstore/    # remote local do DVC (nao versionado)
```

- [ ] **Step 5: Atualizar `docs/dataset.md` e o tracking**

Em `docs/dataset.md`, acrescentar à seção de obtenção que, após o primeiro
`dvc push`, o caminho recomendado é `uv run dvc pull`, e que o download direto
permanece como fallback.

Em `docs/specs/phase-tracking.md`, na Etapa 3, acrescentar o adendo:

```markdown
### Adendo (2026-09-02) — DVC, uv e Python 3.12

- [x] Piso único de Python 3.12 ([ADR-0008](../ai/adr/0008-piso-unico-de-python.md))
- [x] uv com lock único e grupos PEP 735 ([ADR-0007](../ai/adr/0007-uv-com-lock-unico.md))
- [x] Pipeline DVC reprodutível ([ADR-0006](../ai/adr/0006-dvc-como-fonte-de-verdade-do-pipeline.md))

Fora da rubrica da Fase 3 (DVC pertence à Fase 2); adotado para eliminar a
divergência de `numpy` entre treino e inferência e centralizar os
hiperparâmetros. **A Etapa 4 (ONNX/benchmark, 20% da nota) permanece a maior
pendência.**
```

- [ ] **Step 6: Avaliar o gate de aborto do commit 3**

Rodar, nesta ordem:

```bash
make lint && make test
make dev-airflow  # + trigger da DAG, conferir 3/3 verdes
git push          # conferir o CI verde no GitHub Actions
```

**Manter** o commit 3 se os três estiverem verdes e nenhuma alteração em
`docker-compose.airflow.yml` tiver sido necessária.

**Reverter** caso contrário:

```bash
git log --oneline   # identificar os commits das Tasks 7 a 12
git revert --no-commit <sha_task7>..<sha_task12>
git commit -m "revert(dvc): abortar adocao do DVC pelo gate do plano

Gate definido em docs/ai/specs/2026-09-02-dvc-uv-python312-design.md (§6.1).
Motivo: <descrever o que ficou vermelho>. Python 3.12 (commit 1) e uv
(commit 2) permanecem."
```

Registrar o motivo no `phase-tracking.md` e mudar o Status da ADR-0006 para
**Rejeitado**, preservando o documento — a análise continua válida como
registro.

- [ ] **Step 7: Commit**

```bash
git add docs/ README.md
git commit -m "docs(dvc): registrar ADR-0006 e documentar o pipeline DVC

Adiciona a secao de uso do DVC ao README, atualiza docs/dataset.md para
obtencao via dvc pull e registra o adendo da Etapa 3 no phase-tracking."
```

---

## Verificação final (após os três commits)

| Item | Comando | Esperado |
|---|---|---|
| Suíte + lint | `make test && make lint` | PASS |
| Ambiente limpo reproduz | `rm -rf .venv && uv sync --frozen && make test` | PASS |
| API sobe sem `pandas` | `docker build -t urgensight-api . && docker run -d -p 8000:8000 ...` + `curl /health` | 200 |
| Airflow 3.12 + DVC | `make dev-airflow` + trigger da DAG | 3/3 verdes |
| Pipeline idempotente | `uv run dvc repro` duas vezes | 2ª execução: tudo *up to date* |
| Cache seletivo | alterar `train.max_features` → `uv run dvc repro` | só `train` reexecuta |
| CI | `git push` | todos os jobs verdes |
| Sem atribuição de IA | `git log --format=%B \| grep -ic claude` | `0` |
