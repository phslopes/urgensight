-r requirements.txt
pytest>=8.0
# TestClient do FastAPI 0.110+ exige httpx (usado por starlette.testclient).
# Nome do pacote corrigido: "httpx2" era um pacote PyPI distinto e nao
# relacionado (nao expõe o modulo "httpx"), o que so nao quebrava
# localmente porque pyproject.toml ja declara "httpx>=0.27.0" e ambientes
# uv/pip -e instalavam a partir de la; o CI instala so a partir dos
# requirements*.txt e expunha o ImportError.
httpx>=0.28
