pandas>=2.1.4
numpy>=1.26,<2.0
scikit-learn==1.5.1
requests>=2.31
joblib>=1.3
# apache-airflow (Dockerfile.airflow) pulls in dill as a transitive dep and it
# ends up referenced by pickles saved from that environment; keep it installed
# everywhere so models/model.pkl loads regardless of where it was trained.
dill>=0.3.1
# API REST de inferencia (Etapa 2 - src/app.py)
fastapi>=0.110
uvicorn[standard]>=0.29
# Instrumentacao de metricas (Etapa 3 - src/metrics.py)
prometheus-client>=0.20
